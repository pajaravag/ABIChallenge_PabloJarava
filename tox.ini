[tox]
isolated_build = True
envlist = py312
skipsdist = True
 
[testenv]
deps =
    pytest
    pytest-cov
    -r requirements.txt
commands = pytest --cov=src --cov-report html:coverage_html --cov-report xml:coverage.xml --cov-config=tox.ini --cov-branch
 
